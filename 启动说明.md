# NetSketch 启动说明

## 重启电脑后启动

1. 打开 Windows 的 PowerShell（开始菜单搜索 PowerShell）。
2. 依次执行以下命令：

```powershell
cd "D:\AI项目\网络拓扑绘画工具"
npm start
```

3. 看到 `NetSketch: http://localhost:4173`，表示服务启动成功。
4. 在浏览器打开 [http://localhost:4173](http://localhost:4173)。

使用期间保持 PowerShell 窗口开启，可以最小化。电脑重启后需重新执行以上步骤。

## 停止服务

在启动服务的 PowerShell 窗口按 Ctrl+C，或关闭该窗口。下次使用时重新启动即可。

## 首次在其他电脑使用

1. 安装 [Node.js](https://nodejs.org/) 的 LTS 版本，安装后重新打开 PowerShell。
2. 下载 GitHub 仓库源码并解压，或使用 Git 克隆项目。
3. 进入实际项目文件夹，执行 `npm start`。此项目无需安装第三方依赖，无需执行 `npm install`。
4. 浏览器打开 [http://localhost:4173](http://localhost:4173)。

上面的 D 盘路径是当前电脑的项目位置；换电脑或移动文件夹后，请改成实际路径。

## 保存和恢复拓扑

- 浏览器会自动保存最近草稿。使用同一浏览器、同一浏览器用户配置和同一地址打开，通常可恢复草稿。
- 重要拓扑请点击“保存到本地”，下载 `.netsketch.json` 文件。之后使用“打开项目”恢复。
- 清除浏览器数据、更换浏览器或使用隐私模式可能导致草稿不可用，本地项目文件可作为备份。
- PNG/SVG 用于图片分享；继续编辑请保存项目文件。

## 常见问题

**提示找不到 npm 或 node：** 安装 Node.js 后重新打开 PowerShell，可执行 `node -v` 检查。若提示禁止运行 npm.ps1，请使用 `npm.cmd start`，无需修改系统执行策略。

**提示找不到 package.json：** 当前目录不正确。先用 `cd` 进入包含 `package.json`、`server.cjs` 和 `index.html` 的项目文件夹。

**提示 EADDRINUSE（端口被占用）：** 先访问 localhost:4173，可能已有本项目服务运行。若该端口被其他程序占用，可在 PowerShell 中改用其他端口：

```powershell
$env:PORT = "4174"
npm start
```

然后访问 http://localhost:4174。更换端口会使用不同的浏览器草稿存储位置，可通过“打开项目”载入之前保存的项目文件。

**页面打不开：** 确认服务终端仍在运行且没有报错，地址使用 `http://localhost:4173`，不要使用 https。

**能否给其他电脑访问：** 当前服务只监听本机地址，不开放局域网访问。其他电脑可下载源码自行启动，或使用导出的项目文件和图片。

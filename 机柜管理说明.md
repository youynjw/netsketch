# 机柜管理

启动 NetSketch 后，点击拓扑页面右上角「机柜管理」，或直接打开 http://localhost:4173/racks.html 。不需要登录，两个页面使用独立草稿和项目文件。

## 记录机柜落位

1. 左侧点击「＋」新建机房，再点击「新建机柜」。机柜默认 42U，可设置 1–60U。
2. 点击空闲 U 位或「上架设备」，填写名称、起始 U、占用高度和占用面。起始 U 是设备底部，例如起始 20、高度 2 表示 U20–U21。
3. 完整服务器等设备选择「前后贯通」；浅层配线架、盲板等可选择「仅前侧」或「仅后侧」。正面、背面视图分别显示对应设备，贯通设备两面都显示。U 位利用率按实际占用的 U 编号去重统计。
4. 拖动设备调整位置，松手位置对应设备顶部。也可以在属性中直接修改起始 U。超出范围或与同一占用面的设备冲突时不会保存。
5. 在右侧编辑品牌、型号、管理 IP、序列号、归属、备注和颜色，修改后自动保存。设备颜色用于自行分类。
6. 「移动到机柜」可以跨机房移动。跨机柜移动会解除原电源连接，需要重新记录目标 PDU 接线；有接线时拖到其他机柜会先显示移动表单。

## PDU 与插座

- 选中机柜后在右侧点击「＋ 添加」，或点击机柜底部「＋ PDU」，添加 PDU。支持多条 PDU，每条 1–64 个插座，可设置左侧或右侧显示。
- 可记录 PDU 名称、型号、供电来源（例如 A 路 UPS）、额定电流和插座类型（C13、C19、国标五孔或混合 / 其他）。混合型具体插座规格可写在接线备注中。
- 选择设备后，在「电源接线」点击「＋ 连接」，为 PSU1、PSU2 等接口选择 PDU 和插座；也可以直接点击空闲插座连接设备。
- 白色为空闲，已连接插座使用对应设备的颜色，斜纹为停用。选中设备时，其关联插座会有紫色描边。同一插座不可重复占用，停用插座不可连接。
- 插座详情支持解除连接、停用和恢复。PDU 有连接时不可删除；缩减插座数量时不可移除已连接插座。
- PDU 两侧图示不占用 U 位。横装 PDU 若实际占用机柜空间，可同时建立一台「其他」类型设备来记录 U 位。
- 插座状态和额定电流均为人工记录，不采集实时通断电、电流或功率，不计算负载告警。示例中的 A / B 路电源接线仅演示用，请按实际核对。

## 保存、导出与恢复

- 草稿自动保存在当前浏览器，机柜项目与原拓扑项目互不覆盖。
- 点击「保存到本地」下载 `.racks.json`，之后用本页面「打开项目」恢复。新建、载入示例或打开其他项目会替换当前记录，操作前可先保存文件，也可使用撤销恢复。
- 「导出表格」生成 Excel 可打开的 CSV，含设备落位及电源连接，一路电源一行；无电源记录的设备也会导出。该表用于查看，不支持 CSV 导入；继续编辑请使用 `.racks.json`。
- 「打印」打印当前机房、当前正面或背面视图，可在浏览器打印界面保存为 PDF。
- Ctrl+S 保存项目；Ctrl+Z 撤销；Ctrl+Shift+Z / Ctrl+Y 重做。文本输入时撤销快捷键交由输入框处理。
- 左侧「载入示例机房」提供类似原 Excel 的三台 42U 机柜，包含设备和演示用 PDU 接线。

## 同 U 多设备与图片导出

- 顶部以统一的「网络拓扑 / 机柜管理」导航切换页面，文件按钮分别操作当前页面的项目。
- 每台设备可选择「全宽、左半宽、右半宽」。左右位置固定以机柜正面为参照，背面视图自动镜像显示。旧项目设备默认全宽。
- 同一 U 位允许左右半宽并排；仅前侧和仅后侧可独立放置。在不冲突的情况下，同 U 最多可记录前左、前右、后左、后右四台半宽浅设备。前后贯通会占用前后同一宽度位置，全宽会占用左右两半。实际摆放仍需按设备深度、托盘及散热条件核实。
- U 位占用统计按被占用的 U 编号计数，半宽或前后共享同 U 时不会重复累计。两侧显示相同 U 位编号。
- 「导出 SVG / 导出 PNG」导出当前机房当前视角的完整机柜与 PDU，不受滚动位置和搜索高亮影响；半宽位置、背面镜像和插座颜色同步导出。
- 选中设备后按 Del 可直接下架并释放其电源连接，Ctrl+Z 可整体恢复。在输入框中编辑文字或打开弹窗时，Del 不执行下架。

## 颜色刷

选中已有颜色的设备，点击工具栏「颜色刷」，再连续点击其他设备即可复制颜色；对应的 PDU 插座同步更新。再次点击「结束刷色」或按 Esc 退出。每次刷色可独立撤销，同色设备不会产生多余撤销记录。颜色刷仅复制设备颜色，不复制其他属性；修改设备颜色后即可使用。

设备属性修改后自动保存到浏览器草稿，无需点击保存按钮。无效的 U 位、冲突位置或空名称不会保存，离开该字段时恢复原值。输入过程中保持焦点和光标位置。

机柜卡片顶部名称可以直接点击编辑，修改后自动保存；回车结束编辑，名称留空时恢复原名称，支持撤销。

## 画布空间与统计

两侧栏通过靠近画布的 10px 边缘竖条收起或展开，内容滚动时竖条始终可见。统计信息紧凑显示在页面名称旁。点击机柜卡片空白处、名称或左侧机柜条目可以选中机柜，顶部显示其设备数、U 位占用和插座占用，右侧显示机柜属性；点击「查看机房合计」或画布空白处返回整个机房统计。

## 全屏机柜工作区

点击工具栏「全屏」，隐藏网站顶部导航，保留机房信息、统计和操作按钮。进入时左右侧栏自动收起，可通过边缘竖条随时展开。点击「退出全屏」或按 Esc 退出，恢复进入前的侧栏状态。浏览器不支持系统全屏时，使用页面内全屏布局。
